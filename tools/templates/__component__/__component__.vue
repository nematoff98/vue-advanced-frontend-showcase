<template>
  __component__(pascalCase) created!
</template>
<script lang="ts" setup>
import { ref } from 'vue';
import { __component__(pascalCase), __component__(pascalCase)ServiceInstance } from '@/services/__component__(kebabCase)';

interface __component__(pascalCase)Props {
  value: __component__(pascalCase)
}

interface __component__(pascalCase)Emits {
  (e: 'update:value', value: __component__(pascalCase)): void,
}

const props = defineProps<__component__(pascalCase)Props>();
const emits = defineEmits<__component__(pascalCase)Emits>();
</script>
<style lang="scss" scoped>
</style>
