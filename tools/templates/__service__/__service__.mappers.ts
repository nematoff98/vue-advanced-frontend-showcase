import { __service__(pascalCase), __service__(pascalCase)External } from './__service__.interface';

export const __service__(camelCase)ExternalMapper = (item: __service__(pascalCase)External): __service__(pascalCase) => {
  return {
    exampleParam: item.example_param,
  }
}
